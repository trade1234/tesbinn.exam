import { useEffect,useState } from "react";
import { Award,Download,Edit3,Search,Share2,Trash2 } from "lucide-react";
import { useNavigate,useParams } from "react-router-dom";
import CertificateCanvas,{certificateBlob} from "../components/CertificateCanvas.jsx";
import Modal from "../components/Modal.jsx";
import { api } from "../services/api.js";
import { useAuth } from "../context/AuthContext.jsx";

export default function Certificates(){
 const {id}=useParams(),navigate=useNavigate(),{isAdmin}=useAuth(); const [items,setItems]=useState([]),[selected,setSelected]=useState(null),[search,setSearch]=useState(""),[editing,setEditing]=useState(null);
 const load=()=>api.get("/certificates"+(search?"?search="+encodeURIComponent(search):"")).then(r=>{setItems(r.data);if(id){const found=r.data.find(x=>x._id===id);if(found)setSelected(found);else api.get("/certificates/"+id).then(x=>setSelected(x.data));}});
 useEffect(()=>{load()},[id]);
 async function download(c){const blob=await certificateBlob(c);await api.post("/certificates/"+c._id+"/download");const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=c.certificateId+".png";a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
 async function share(c){const blob=await certificateBlob(c),file=new File([blob],c.certificateId+".png",{type:"image/png"});if(navigator.canShare?.({files:[file]}))await navigator.share({title:"My professional certificate",text:"I earned a certificate from "+c.companyName,files:[file]});else{await navigator.clipboard.writeText(window.location.href);window.alert("Certificate link copied.");}}
 async function saveEdit(e){e.preventDefault();const {data}=await api.put("/certificates/"+editing._id,editing);setEditing(null);setSelected(data);load()}
 async function remove(c){if(!window.confirm("Remove certificate "+c.certificateId+"?"))return;await api.delete("/certificates/"+c._id);if(selected?._id===c._id)setSelected(null);load()}
 return <div className="space-y-6">
  {selected&&<section className="overflow-hidden rounded-3xl bg-gradient-to-br from-[#082b55] via-[#0b5cab] to-[#1594dc] p-5 text-white shadow-xl sm:p-9">
   <div className="mx-auto mb-6 max-w-2xl text-center"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-white/15"><Award size={34}/></div><p className="mt-4 text-sm font-bold uppercase tracking-[.25em] text-sky-200">Achievement unlocked</p><h1 className="mt-2 text-3xl font-bold sm:text-4xl">Congratulations, {selected.studentName}!</h1><p className="mt-3 text-blue-100">You passed {selected.examName} with {selected.percentage}%. Your verified certificate is ready.</p></div>
   <div className="mx-auto max-w-5xl bg-white p-2 sm:p-4"><CertificateCanvas certificate={selected}/></div>
   <div className="mt-6 flex flex-wrap justify-center gap-3"><button className="btn-primary bg-emerald-600 hover:bg-emerald-700" onClick={()=>download(selected)}><Download size={18}/> Download PNG</button><button className="btn-secondary" onClick={()=>share(selected)}><Share2 size={18}/> Share certificate</button><button className="btn-secondary" onClick={()=>setSelected(null)}>View all certificates</button></div>
  </section>}
  {!selected&&<><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><h1 className="text-2xl font-bold">{isAdmin?"Issued Certificates":"My Certificates"}</h1><p className="text-sm text-slate-500">{isAdmin?"Search, download, update, or remove issued credentials.":"Your verified professional achievements in one place."}</p></div>{isAdmin&&<form className="flex gap-2" onSubmit={e=>{e.preventDefault();load()}}><label className="relative"><Search className="absolute left-3 top-3 text-slate-400" size={17}/><input className="input pl-9" placeholder="Name or certificate ID" value={search} onChange={e=>setSearch(e.target.value)}/></label><button className="btn-primary">Search</button></form>}</div>
  <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.map(c=><article key={c._id} className="card overflow-hidden p-4"><CertificateCanvas certificate={c}/><div className="mt-4"><p className="font-bold">{c.studentName}</p><p className="text-sm text-slate-500">{c.courseName}</p><p className="mt-1 font-mono text-xs text-blue-700">{c.certificateId}</p><div className="mt-4 flex flex-wrap gap-2"><button className="btn-primary" onClick={()=>{setSelected(c);navigate((isAdmin?"/admin":"/student")+"/certificates/"+c._id)}}>View</button><button className="btn-secondary" onClick={()=>download(c)}><Download size={15}/></button>{isAdmin&&<><button className="btn-secondary" onClick={()=>setEditing({...c})}><Edit3 size={15}/></button><button className="btn-secondary text-red-600" onClick={()=>remove(c)}><Trash2 size={15}/></button></>}</div>{isAdmin&&<p className="mt-3 text-xs text-slate-400">{c.downloadCount} downloads " Issued {new Date(c.issueDate).toLocaleDateString()}</p>}</div></article>)}</div>{!items.length&&<div className="card p-10 text-center text-slate-500">No certificates found.</div>}</>}
  {editing&&<Modal title="Update Certificate" onClose={()=>setEditing(null)}><form className="space-y-3" onSubmit={saveEdit}>{["studentName","courseName","examName","companyName","signatoryName"].map(k=><label key={k} className="block text-sm font-semibold capitalize">{k.replace(/([A-Z])/g," $1")}<input className="input mt-1" value={editing[k]||""} onChange={e=>setEditing({...editing,[k]:e.target.value})}/></label>)}<div className="flex justify-end gap-2"><button type="button" className="btn-secondary" onClick={()=>setEditing(null)}>Cancel</button><button className="btn-primary">Save changes</button></div></form></Modal>}
 </div>
}
